# AI 工作台实现对照 v1

| 对照项 | 概念稿 | 当前实现 | 结论 |
| --- | --- | --- | --- |
| 框架 | 顶栏、项目/会话左栏、中央对话、右侧轨迹、底部状态 | 1586 × 992 下五区域对齐 | 一致 |
| 层级 | 纯白主画布，浅灰侧栏，1px 分隔线 | 没有额外卡片网格，主次区域依靠底色和分隔线 | 一致 |
| 品牌与状态 | YEUTECH、AI 工作台、蓝色强调、底栏连接状态 | 标题、选中态、轨迹状态和 `OpenCode · 已连接` 完整保留 | 一致 |
| 会话交互 | 左栏新建会话，中央发送/停止 | 新会话独立消息状态；发送后标题、选中会话和消息同步；停止退出运行态 | 一致 |
| 辅助面板 | 轨迹、上下文、计划三栏 | 三个标签可切换，上下文明示 Project A 受限工作区 | 一致 |
| 响应式 | 窄屏先折叠右栏，手机再折叠左栏 | 390 × 844 下只保留对话主区，无水平溢出，输入区可见 | 一致 |
| 助手消息容器 | 浅灰色弱容器 | 使用更开放的纯白阅读画布，用头像和排版建立层级 | 有意差异：当前方案在长回复下更轻 |

首屏锁定文案已对照 `design-spec.md` 全部出现，无缺失项。
